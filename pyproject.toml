[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = 'plur'
version = '0.21.1'
dependencies = [
  "pexpect>=4.8",
  "mini>=0.2",
  "pydantic>=2.13.3",
]

[tool.uv.sources]
mini = { git = "https://github.com/pluripotency/mini" }
